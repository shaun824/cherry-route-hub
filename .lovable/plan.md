# PE Plett booked-extras email

## Build
- Add a branded PE Plett email that summarises only the extras on that rider's entry.
- Show each booked extra with a plain-language explanation and exact redemption, collection, or use instructions.
- Add the rider's personalised Entry Ninja registration link for entry management.
- Make the content dynamic from the extras already synced against each entry, with safe fallbacks for unknown items. Add a clearly separate section highlighting other extras still available, with the rider’s Entry Ninja booking link and the PE Plett optional-extras website link.

## Workflow
- Add this email as a reusable PE Plett workflow step without activating a bulk send.
- Ensure one recipient receives one message, with duplicate-send protection.
- Send Shaun a test populated with every currently documented PE Plett extra.

## Verification
- Confirm the email renders cleanly on mobile and desktop, links work, and the test is accepted for delivery.
